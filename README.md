# MTCNN FACE DETECTOR

* run recorded video using ```mtcnn_cv2_video.py``` file
* ```python mtcnn_cv2_video.py -v <input video file> -o <output video file>```
* Note for running ```mtcnn_model_test.py``` tensorflow is needed and the processing is slow